# Frontend Environment Variables
# Copy to .env.local for local development

# API URL - Backend server endpoint
# In development: Leave empty or unset (uses Vite proxy to localhost:8000)
# In production: Set to your deployed backend URL (e.g., https://api.your-domain.com)
VITE_API_URL=
